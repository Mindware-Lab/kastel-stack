# Domains

Each domain owns:
- inbound contract handling
- domain-specific workflows
- outbound contract publication
- domain SLOs and runbooks

All cross-domain communication must use versioned contracts from `contracts/`.